name: my_portfolio
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.3.2
  http: ^1.2.2
  mailer: ^6.1.2
  path_provider: null
  permission_handler: any
  scrollable_positioned_list: null
  url_launcher: ^6.3.0
  animate_do: ^3.3.4
  animated_text_kit: ^4.2.2
  dio: null

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
